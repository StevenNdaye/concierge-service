weather:
  url: http://localhost:8089
  api-secret: some-test-api-key
customer:
  url: http://localhost:8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/concierge
    username: stevenndaye
    password:
    platform: POSTGRESQL
    initialization-mode: always
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
    generate-ddl: true